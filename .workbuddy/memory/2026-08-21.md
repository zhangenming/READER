# 2026-08-21

- 将内置双字词连词标记（如「如果」中间的衔接圆点）从黑色实心改为白色空心圆点：修改 `styles.css` 中对应 `::after` 伪元素的 `background: #000000` 为 `background: transparent; border: 1.5px solid #ffffff`，并提交 git。
- 因浏览器缓存未生效，更新 `index.html` 中 `styles.css` 的版本号从 `?v=20260820-05` 到 `?v=20260821-01`，使新样式生效。
- 用户反馈空心圆点边框太细，从 `1.5px` 加粗到 `3px`，并同步 bump 版本号至 `?v=20260821-02`。
- 用户反馈圆点太大，将尺寸从 `0.3em` 缩小到 `0.22em`、位置偏移从 `-0.15em` 调整到 `-0.11em`，并 bump 版本号至 `?v=20260821-03`。
- 用户要求改成白色实心并再小点，去掉白色边框、改 `background: #ffffff`、尺寸缩到 `0.16em`、偏移 `-0.08em`，bump 版本号至 `?v=20260821-04`（注意：白色实心在米白纸面/浅段落底色上可能几乎不可见）。
- 用户反馈圆点稍小，将尺寸从 `0.16em` 放大到 `0.2em`、偏移从 `-0.08em` 调到 `-0.1em`，bump 版本号至 `?v=20260821-05`。
