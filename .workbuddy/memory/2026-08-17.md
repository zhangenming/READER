# 2026-08-17

- 实现「双击正文行自动复制整行」功能：在 `app.js` 的 `处理高亮双击` 中增加非关键词命中的行双击分支，调用新增 `选中并复制行` 函数，通过 `Range.selectNodeContents` 选中该行 DOM 并通过 `navigator.clipboard.writeText` 写入原文行文本；同步将 `index.html` 中 `app.js` 缓存版本号提升到 `20260817-04`。已提交 commit `888e617`。
- 实现「选中关键词自动向右扩展」功能：在 `app.js` 新增 `尝试自动扩展关键词` 函数，在 `读取选择关键词` 中先对选中的 trimmed 文本进行循环扩展——每次追加选中位置后的下一个字符，若扩展前后词的全文命中次数相同且均大于 1，则确认扩展并继续下一轮；否则停止。最终将扩展后的词作为关键词添加或删除。同步将 `index.html` 中 `app.js` 缓存版本号提升到 `20260817-06`。
