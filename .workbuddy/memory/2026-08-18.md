# 2026-08-18

## 内置关系连词表扩展（果然/忽然/莫非）
- 需求：把「果然」「忽然」「莫非」加进内置关系连词表（app.js `关系连词表`，97 行附近）。
- 归类：果然→果词（现有类）；莫非→假设词（现有类）；忽然→**新建 `时间词` 类**（不属于原有任何逻辑类别）。
- 关键机制：关系连词表只支持 **2 字词**（消费逻辑 `词.length === 2`，app.js:4778）；命中后在两字之间画黑色连接线（styles.css 965-995），**不是**彩色高亮（彩色高亮是用户选词建的"关键词"，两套机制）。
- 新增类别必须同步两处 CSS：`.字.时间词:not(.命中){box-shadow:none}` 段 + `:has(+ .字.时间词)::after` 连接线段。
- 已有硬编码连词（不走词表，勿重复加）：但是/不过/还是（转折词，app.js:4765）、虽然/虽说/尽管/即使（让步词，app.js:4761）。
- 提交：6c6ab0d，仅 app.js + styles.css。

## 内置双字词连接符改回纯黑直线
- 需求：用户参考截图（表格顶部黑色横线），要求优化双字词之间的"顶部分割线"效果。
- 修改：styles.css 965-997，将 `让步词/转折词/因词/果词/假设词/递进词/选择词/顺接词/时间词` 两字之间的连接符从实心圆（0.24em）改回纯黑直线。
- 参数：宽度 0.6em、高度 2px、right -0.3em（居中于两字之间）、border-radius 0（无圆角），命中态仍由高亮接管。
- 提交：784a1f0，仅 styles.css。
- 缓存：同步将 index.html 中 styles.css 版本号从 `v=20260817-03` 提升到 `v=20260818-01`，提交 a67c959。

## 修正：顶部分割线实际指分析表格顶部边框
- 反馈：用户指出上次改错地方，"顶部的分割线"应指查找弹窗中分析表格容器顶部的分隔线，不是双字词连接线。
- 撤销：styles.css 965-997 恢复为实心圆（与 c06e46d 一致）。
- 修改：styles.css 1659-1679，去掉 `.分析结果` 的 `border-top`，将 `.分析表格容器` 的 `border-top` 改为 `1px solid #000000`。
- 缓存：index.html 中 styles.css 版本号从 `v=20260818-01` 提升到 `v=20260818-02`，提交 85e5c0a。
