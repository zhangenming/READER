# 2026-08-05

## 数字醒目样式：扩展量级汉字
- 在 `app.js` 的 `数字字素模式` 中加入 `十百千万亿`，使中文数字量级单位也套用 `.字.数字` 醒目样式（强调红 + 加粗 + 文字阴影，无背景色）。
- 这些字符为方块字、逐字成 `.字`，按字符判定即可；如 "十二""一千""三万" 中每个数字字符各自高亮。
- 小副作用："十分"(很)、"百万年" 里的 十/百万 也会高亮（量级字几乎只见于数字语境，可接受）。
- `index.html`：app.js 缓存版本号 +1（?v=20260803-29）。样式（文字阴影方案）沿用 08-03 的修订，未再改 CSS。

## 自动滚动 / 空格翻页隐藏鼠标光标
- `styles.css`：新增 `body.自动滚动中, body.自动滚动中 *` 规则 `cursor: none`。
- `app.js` `开始自动滚动()`：加 `body.classList.add('自动滚动中')`。
- `app.js` `停止自动滚动()`：移除该 class。
- `app.js` 空格翻页路径（非自动滚动时的 `按整页滚动`）：也加 `自动滚动中` class 隐藏光标。
- `app.js` `处理鼠标移动()`：重构为——自动滚动中走原有逻辑（mousemove 停止滚动）；非自动滚动时移除 `自动滚动中` class（空格翻页隐藏的光标在鼠标移动时恢复）。
- `index.html`：css 版本 → 36，js 版本 → 30。
