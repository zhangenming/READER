# 2026-08-12

## 正文「我」→「W」显示层替换
- 用户要求把正文里所有汉字「我」替换成英文字母「W」，明确为「显示层替换」：书本 .txt 原文件不动，仅阅读渲染时显示 W。
- 改动位于 `app.js` 正文逐字渲染处（`渲染可见行` → 创建行片段 的 `字元素.textContent` 赋值，约 line 3614）：`字文本 === '我' ? 'W' : 字文本`。
- 关键约束：`状态.文本` 保持原「我」不变（关键词生成 line 3912、双击跳转、定位均按位置偏移读 `状态.文本`，不读 DOM 文字），故词频统计、跳转、高亮均不受影响，只改可见字形。

## 让步词/转折词高亮词表扩展
- `app.js` 正文逐字渲染处（约 line 3635）用「首字+邻字」邻接判定给 span 加类：`让步词`（暖橙 oklch 75°）、`转折词`（冷青 oklch 165°）。
- 本次扩为三组配对：虽然→但是、尽管→还是、即使→不过。前一半归「让步词」，后一半归「转折词」（配色语义：让步暖、转折冷）。
- 边界约定：「不过」是凝固连词，其首字「不」不参与「否定叉 ✕」遮罩（修改了 line ~3618 的 `否定叉` 判定：后邻为「过」时跳过），避免把连词误标成否定。
- 改 JS 后须提升 `index.html` 中 `app.js?v=` 版本号（本次 08→09）并硬刷新。

## 关系连词高亮新增 5 类（数据驱动词表）
- 用户追加：因为…所以、由于…因此、既然 / 如果 只要 只有 / 不但…而且、甚至、况且 / 或者 / 然后 继续。
- 新增 6 个语义类（app.js 顶部 `关系连词表` 数组 + 渲染循环查表着色；样式在 styles.css，各用独立色相）：
  - 因词（红 25°）: 因为、由于、既然；果词（紫 295°）: 所以、因此
  - 假设词（蓝 230°）: 如果、只要、只有
  - 递进词（绿 140°）: 不但、而且、甚至、况且
  - 选择词（品红 330°）: 或者
  - 顺接词（青 200°）: 然后、继续
- 视觉约定：前提/条件/选择类用点状下划线（同 让步词），结论/递进续/顺接类用内嵌下划线（同 转折词）。
- `关系连词表` 为 2 字词「首字+邻字」邻接判定；词表后续增删只改该数组，不动渲染逻辑。已用 Node 脚本逐字验证全部 17 词命中正确。
- 版本号：app.js 09→10、styles.css 02→03（均需硬刷新）。

## 补充 然而 / 于是
- 用户追加：然而（转折词，冷青）、于是（果词，紫）。均为 2 字，直接进 `关系连词表`；邻接判定无串色（然靠后邻「而」、于靠后邻「是/因」、是靠前邻「于/但/还」区分）。
- app.js?v= 10→11；styles.css 未改类，不动。已 Node 脚本验证全部 21 词命中正确。
